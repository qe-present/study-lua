name: Lua run
on:
  workflow_dispatch:
    inputs:
      num:
        description: 输入数字
        type: number
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 获取仓库代码
        uses: actions/checkout@v4

      - name: 安装lua
        uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.4"

      - name: 查找目录
        id: find
        run: echo "dir_path=$(bash script/find_dir.bash "${{ github.event.inputs.num }}")" >> "$GITHUB_OUTPUT"

      - name: 运行 Lua
        run: |
          cd "${{ steps.find.outputs.dir_path }}"
          lua main.lua
